Place diffusion enhancement checkpoints here.

Expected default path:

```text
diffusion/ckpt/enhancer.ckpt
```

The checkpoint is not included in this repository. Download it using the project release instructions or update `configs/stage2/diffusion/enhancer.yaml` to point to your local checkpoint.
